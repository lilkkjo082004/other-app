name = "other-ai"
main = "src/worker.js"
compatibility_date = "2024-11-01"

# Lock CORS to your deployed web app's origin in production, e.g.
#   "https://other.example.com". Leave unset to allow any origin (handy in dev).
[vars]
# ALLOWED_ORIGIN = "https://your-web-app.example.com"

# The Anthropic API key is a SECRET, not a var. Set it with:
#   wrangler secret put ANTHROPIC_API_KEY
